parameters:
 - name: terraformWorkspace
   type: string
 - name: serviceConnection
 - name: rootModuleFolderRelativePath
   default: '.'

steps:
- task: AzureCLI@2
  displayName: Terraform Set Workspace
  inputs:
    azureSubscription: ${{ parameters.serviceConnection }}
    scriptType: pscore
    scriptLocation: inlineScript
    addSpnToEnvironment: true
    inlineScript: |
      $workspaceName="${{ parameters.terraformWorkspace }}"
      $modulePath = Join-Path $env:PIPELINE_WORKSPACE 'src'
      $rootModuleFolderRelativePath = "${{ parameters.rootModuleFolderRelativePath }}"
      if ($rootModuleFolderRelativePath -and
        $rootModuleFolderRelativePath -ne '.' -and
        $rootModuleFolderRelativePath -ne '') {
        $modulePath = Join-Path $modulePath $rootModuleFolderRelativePath
      }
            
      # Change to module path before running workspace commands
      Push-Location $modulePath
            
      # Check if the workspace exists
      
      $existingWorkspaces = terraform workspace list

      if ($existingWorkspaces -match $workspaceName) {
          terraform workspace select $workspaceName
      } else {
          terraform workspace new $workspaceName
          terraform workspace select $workspaceName
      }